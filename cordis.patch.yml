# dsh-web-scroll-flow bundle patch: 向 Web 组合插入浏览器插件行。
#
# 安装：pnpm dsh plugin --profile web add /mnt/TY/dsh/dsh-web-scroll-flow
# 卸载：pnpm dsh plugin --profile web remove dsh-web-scroll-flow
- insert:
    - id: scroll-flow
      name: dsh-web-scroll-flow
